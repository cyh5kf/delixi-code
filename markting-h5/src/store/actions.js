import * as types from './mutation-types';
import localStorage from './localstorage';

// export const saveUserName = function ({ commit }, username) {
// 	localStorage.setUserName(username);
// 	commit(types.SAVE_USER_NAME, username);
// }

export {}
